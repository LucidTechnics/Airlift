//$generatorComment$
//$dateString$

var $className$Validator;

if (!$className$Validator)
{
	$className$Validator = {};
}
else if (typeof $className$Validator != "object")
{
	throw new Error("$className$Validator already exists and it is not an object");
}

$validationFunction; separator="\n\n"$
